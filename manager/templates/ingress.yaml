apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{APP_NAME}}-{{APP_VERSION}}-ingress"
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  rules:
  - host: "{{APP_NAME}}-{{APP_VERSION}}.ideabrowse.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: "{{APP_NAME}}-{{APP_VERSION}}"
            port:
              number: 80
  tls:
  - hosts:
    - "{{APP_NAME}}-{{APP_VERSION}}.ideabrowse.com"
    secretName: "{{APP_NAME}}-{{APP_VERSION}}-tls"
